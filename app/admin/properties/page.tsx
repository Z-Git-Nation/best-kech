export default function PropertiesAdmin() {
  return (
    <div>
      <h1 style={{ fontFamily: 'var(--font-cormorant)', fontSize: '2rem', color: 'var(--terracotta)', marginBottom: '1.5rem' }}>
        Villas & Appartements
      </h1>
      <p style={{ color: 'var(--text-mid)', fontSize: '0.9rem' }}>
        Interface de gestion — à construire avec les composants UI.
      </p>
    </div>
  );
}
